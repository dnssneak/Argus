version: '3.8'

services:
  argus:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: argus_security_platform
    ports:
      - "5001:5001"
    env_file:
      - .env
    environment:
      - PORT=5001
      - FLASK_DEBUG=False
    restart: unless-stopped
